[project]
name = "modbus-suite"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = ">=3.10"
dependencies = [
    "pymodbus",
    "requests",
    "paho-mqtt",
]
